class Solution {
public:
    bool judgeSquareSum(int c) {
        for(long i=0;i*i<=c;i++){
            double temp =  sqrt(c-i*i);
            if(temp == (int)temp){
                return true;
            }  
        }
          return false;
    }
  
};
